# Demo mode is fully offline and requires no credentials.
# Optional: override the deterministic analysis reference timestamp.
ANALYSIS_AS_OF=

# Phase B live providers. Demo mode does not require any of these values.
APIFY_TOKEN=
GOOGLE_SERVICE_ACCOUNT_JSON=

# Reserved for a future optional offer-enhancement provider. Phase B MVP uses
# deterministic templates even when these values are populated.
LLM_PROVIDER=
LLM_MODEL=
LLM_API_KEY=
